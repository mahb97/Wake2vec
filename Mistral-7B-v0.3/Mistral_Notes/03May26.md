# wake2vec devlog 2026-05-03

## Mistral 7B v0.3 P1 session 14 (resuming from step 1100, *not* 1200)

infrastructure note: the SentryMirror at step 1200 wrote partial files (no `adapter_model.safetensors`, no model weights), investigation showed step 1150 had the same partial-mirror problem. the EmbeddingSnapshot files in `emb_snaps/` are intact through step 1150, so the actual trained weights are recoverable.

a manual-resume path was sketched (load embeddings directly from `emb_step1150.pt`, skip Trainer-native resume) but rejected on principle. **a loss curve that starts from 0 is not a curve that broke 11.0 yesterday.** continuity matters. the deck said "do not change strategy" which includes the strategy of keeping a clean step counter.

resume from `checkpoint-1100` (last fully-mirrored sentry). re-train steps 1100 through to 1200 with slightly different batch ordering. val should land in the same neighbourhood, embeddings at step 1100 were a few steps shy of the 11.0 break, so the plateau crack at ~1150 will reproduce, possibly at a different exact step but in the same window.

Resuming from `checkpoint-1100`.

### P1 loss table (rolling back)

| Step | Train | Val | Session | Note |
|------|-------|-----|---------|------|
| 1000 | 136.11 | 11.13 | 11 | wish denied |
| 1050 | 136.08 | 11.06 | 12 | |
| 1100 | 133.95 | 11.15 | 12 | last full sentry |
| 1150 | 132.23 | **10.95** | 13 | *first 11.0 break (lost to FUSE)* |
| 1200 | 129.95 | 10.96 | 13 | *consecutive break (lost to FUSE)* |
| 1100+ | | | | *resuming today, session 14, retracing* |

---

## Notes

adding a verification step to SentryMirror that checks for `adapter_model.safetensors` (or model weight file) before claiming a successful mirror. 

---

[Ok :)](https://soundcloud.com/montell2099/ok-2?si=d01ecf537441420d98417e4552ebf704&utm_source=clipboard&utm_medium=text&utm_campaign=social_sharing)
