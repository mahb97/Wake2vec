# wake2vec devlog 2026-06-29

## Mistral 7B v0.3 P2 session 3 (resuming from step 150)

last logged step 100 at val 7.33, train 7.63. the arrow is routing: a -3.58 drop from the P1 best-val source (10.9181), descent-shaped not wall-shaped, and val below train (the rich P1 embeddings generalising rather than overfitting). 

Resuming from `checkpoint-150`. SEQ_LEN 512. next eval at 200.

### P2 loss table

| Step | Train | Val | vs P1 best (10.92) | Note |
|------|-------|-----|--------------------|------|
| 100 | 7.63 | 7.33 | -3.58 | descent-shaped, arrow routing, val below train |
| 200 | 5.82 | 5.547 | -5.37 | -1.78 in one interval, past the 6s into the 5s, val still below train, no floor |

the val read at step 200 is the secondary signal (Mistral is not the wall-decider; the 8B and 3B are). the question is the shape of the descent:

- **keeps falling toward the 6s (like Qwen)**: the arrow routes Mistral's rich micro-units freely, the 58%-cohort model descending like a good cohort model.
- **finds a floor around 7**: a soft settle, still a clean descent from the 10.92 source, no wall.

the val number is not the verdict for Mistral.

### suspension test

Mistral's P2 verdict comes at the generation battery, not the loss curve. the question the whole P1 analysis set up: does the arrow route Mistral's babel into the fine line (TinyLlama-style suspension, novel forms inside recoverable syntax) or keep it dissolved (8B-style over-deformation)? the P1 generation was the maximal-babel pole (the widest register-collision in the lineup, real Wake tokens, emoji at every temperature, but fragmented and unrouted).

Mistral matches TinyLlama's 58% share and did the deepest P1 learning in the lineup, so the prior on it holding suspension is the most favourable of any model. and the step-100 P2 signal supports it: val below train means the configuration generalises, which is the precondition for coherent generation. if any model challenges TinyLlama for the suspension crown, the analysis says it is this one, and the P2 generation is where we find out.
