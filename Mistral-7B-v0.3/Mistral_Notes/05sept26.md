# wake2vec devlog 2026-09-05

> *P3 restarts*

## Mistral 7B v0.3 P3 RELAUNCH from step 0

The run restarts from the P2 best-val checkpoint (step 600, val 4.705) under the configuration established over the preceding week: cosine morpheme loss, lambda_morph 1.0, strip groups excluded, movable pairs only, and the permutation baseline computed 
inside the script before training begins.

### configuration and supervision

| | value |
|---|---|
| Adapter source | `wake2vec_mistral7b_p2_lora/full_checkpoints/step_0600` |
| Embedding source | `embeddings_frozen.pt` |
| Morph loss | cosine direction consistency |
| lambda_morph / lambda_device | 1.0 / 2.0 |
| Supervision | 130 groups, 1,413 pairs, all movable |
| Wake rows touched | 1,125 by morpheme, 2,261 including device, of 44,553 (5.1%) |
| LR / steps | 2e-5 / 1000, patience 3, eval and save every 100 |
| Batch / SEQ_LEN | 1 x 16 / 256, matching P1 and P2 |

The touched-row fraction is the reason the aggregate Wake-region isotropy cannot serve as the primary instrument: any effect confined to those rows is diluted roughly twentyfold in a statistic computed over all 44,553. The treated-versus-control 
measurement addresses this directly, comparing the treated rows against a size-matched untreated control that receives language-modelling gradients but no auxiliary ones.

### baseline

The chance floor is group-size dependent and is recomputed on this supervision set before training. The reference values are:

| | value |
|---|---|
| observed L_morph | 0.669970 (consistency 0.330030) |
| null mean, 500 permutations | 0.715731 (consistency 0.284269) |
| null sd | 0.000928 |
| **excess over chance** | **+0.045761** |
| z | -49.33 |
| groups below z = -3 | 15 of 130, holding 215 pairs (15.2%) |

Excess over chance is the quantity that compares across supervision sets. The structure is concentrated in the Latinate nominalisation cluster: `-ion` (-18.4), `-ation` (-16.7), `-fication`, `-ition`, `-ure`, `-ive`. Slightly over half the groups 
sit at chance. The objective therefore acts on structure present in roughly a seventh of its supervision, and modest movement is the informed expectation.

### from the previous run

Before the previous restart, the run reached step 159 and recorded L_morpheme descending across four logged points:

| step | L_morph | consistency |
|---|---|---|
| 0 | 0.6700 | 0.3300 |
| 50 | 0.6672 | 0.3328 |
| 100 | 0.6546 | 0.3454 |
| 150 | 0.6370 | 0.3630 |

Monotonic and accelerating, against a loss that under the previous formulation remained fixed at the fourth decimal across 2,000 steps and 500x lambda amplification. The morpheme objective moves when correctly scaled and applied to supervision 
capable of receiving gradient. This is the first geometric movement recorded in the project, and reproducing it is the immediate purpose of the restart.

L_device wandered its usual 0.17 to 0.22 band over the same interval with no trend, which is the dissociation the design anticipated: morpheme structure amplifiable, device clustering not.

### Sanity targets for step 100

| | expected | source |
|---|---|---|
| train | 5.180 | logged directly last run |
| **validation** | **5.4765** | last run's 0.342284, corrected by the factor of 16 |
| morph | 0.6700 | matches Cell 8c's `observed L_morph 0.669970` |

### pre-registered outcomes

- **Movement.** L_morph descends and excess over chance increases. The informative form is per-group: if the groups that gain are those that already carried structure, the objective amplifies existing structure rather than creating it,
which is a mechanistic result independent of the aggregate.
- **Null.** L_morph and excess over chance unchanged. A null obtained under a correctly scaled objective, on audited supervision, against a measured chance floor, and therefore different in evidential status from the four that preceded it.
