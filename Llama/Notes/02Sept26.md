# wake2vec devlog 2026-09-02

> *P3 launches on a Wake region measured at the isotropy attractor*

## Llama 3.1-8B P3 LAUNCH 

P3 begins from the P2 best-val checkpoint (step 900, val 5.1486). cosine morpheme loss, λ_morph recalibrated to 1.0, strip-group and frozen-pair filters applied to the supervision.

### configuration

| | value |
|---|---|
| Adapter source | `wake2vec_llama8b_p2_lora/full_checkpoints/step_0900` |
| Embedding source | `embeddings_frozen.pt` (P2 saved embeddings once) |
| Morph loss | cosine direction consistency, the quantity the analysis reports |
| λ_morph / λ_device | 1.0 / 2.0 |
| Supervision filters | strip groups excluded; movable pairs only |
| Optimizer | Adafactor |
| LR / steps | 2e-5 / 1000, patience 3, eval and save every 100 |
| Batch / SEQ_LEN | 1 x 16 / 512, matching P1 and P2 |
| Embeddings | Wake rows unfrozen, base rows gradient-masked |

Adafactor replaces AdamW because the embedding matrix is 172,451 x 4096 = 706M parameters, and the gradient mask zeroes gradients after they are materialised, so the optimizer carries state for the whole matrix: ~5.65GB of fp32 moments, 
which does not fit a T4 alongside a 4-bit 8B body. 

Llama 8B's P2 embedding analysis places the Wake region at 0.998453 isotropy, at the attractor observed across the lineup and above the base vocabulary's own 0.9896, with base-new pairwise cosine 0.0009 and no nearest neighbour above 0.083. 
on the standing structural diagnosis, that near-uniform geometry offers the auxiliary losses nothing to amplify, so the prior on movement is low.

this is the first P3 on a 128K-vocabulary model under a morpheme objective at a scale that can affect the embeddings. it carries the set's only compositional 1.0x initialisation, varying initialisation against every other P3. its P1 analysis 
already separated the two effects: norm structure is initialisation-dependent (Cohen's d -1.09 at the step-1200 matrix, against approximately -7 under spherical), isotropy is not. the P2 generation is inconsistent with the geometric characterisation. 
the battery returned roughly 70% verified Wake-lexicon retrieval (42 confirmed tokens against 16 inventions), predominantly corpus hapaxes, placed in grammatical position and recurring across independent samples. from a region that isotropy, 
pairwise cosine, nearest-neighbour structure and eigenspectrum all describe as featureless. a null here would establish that the geometric instruments do not detect the property responsible, not that the property is absent.

the question here is; does the objective also move a region that entered at the attractor, or is movement confined to the one model that entered below it (0.995)? Mistral supplies the positive case; this run supplies the control.

`MORPH_MOVABLE_ONLY` removes pairs tokenising entirely into base-vocabulary rows, which the gradient mask freezes. this model's 128K base vocabulary means substantially more morpheme words already exist as base tokens than in Mistral's 32K, 
so the filter is expected to reduce the set considerably further than Mistral's 5,924 to 2,922 to 1,413 progression. the surviving pair count is reported at launch; below a few hundred pairs the permutation null loses power, as the chance floor 
rises with 1/sqrt(n) as groups shrink, and a flat L_morph would be uninformative.

the chance floor is group-size dependent and is computed on this model's own supervision before training begins. 

### pre-registered outcomes

- movement. L_morph descends and excess-over-chance increases post-training. this would be the first geometric movement recorded from a region measured at the attractor, and would indicate that the geometric instruments are the limiting factor.
- null. L_morph and excess-over-chance unchanged. a null obtained under a correctly scaled objective on audited supervision, and materially different in evidential status from the four that preceded it.
