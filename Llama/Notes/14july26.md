# wake2vec devlog 2026-07-14

> *the floor-hunt, but no longer cursed*

## Llama 3.1-8B P2 session 12 

the cursed crossing eval landed last session: step 400, val 5.418, still 0.088 above the 3B's frozen 5.33, so the literal sub-5.33 crossing is the next eval (500). the decision stands: not cutting, running to find the floor, because the lowest val is the best-val checkpoint for the P2 suspension battery and the descent shape is finding-relevant.

resuming from `SENTRY/checkpoint-400` with save_steps changed to 10. SEQ_LEN 512, next eval at 500.

### P2 loss table

| Step | Train | Val | vs P1 best (11.36) | Note |
|------|-------|-----|--------------------|------|
| 200 | 6.818 | 6.540 | -4.82 | decider: descent, no wall |
| 300 | 5.839 | 5.723 | -5.64 | into the 5s |
| 400 | 5.357 | 5.418 | -5.94 | crossing eval landed. 0.088 above 5.33, descending |
| 500 | | | | *the sub-5.33 crossing (val), and the floor-hunt continues* |

### note on the save_steps fix

because this is running very slowly and losing 50 steps means way over an hour of lost colab GPU time, the SAVE-STEPS have been changed from 50 to 10. the resume was honouring the checkpoint's recorded `save_steps=50` over the args so this run applies a `trainer_state.json` overwrite to set `save_steps=10` 

```
import json,
p = RESUME_FROM / "trainer_state.json"  
s = json.load(open(p))
print("old Save-steps:", s.get("save_steps"), s.get("logging_steps"))
s["save_steps"] = 10
s["logging_steps"] = 10
json.dump(s, open(p, "w"), indent=2)
print("new save-steps: ", s["save_steps"], s["logging_steps"])
```
