# wake2vec devlog 2026-07-04

> *past the decider, tracing the fall*

## Llama 3.1-8B P2 session 7 (resuming from step 250)

the decider is confirmed: step 200 landed at val 6.540, descent not wall, and the scale-gated-ceiling claim is now controlled by the cleanest comparison in the project (8B holds 128K vocab and Llama architecture constant with the 3B, changes only scale, and descends where the 3B walled).

resuming from `checkpoint-250`. SEQ_LEN 512, ~240s/step, EVAL_STEPS=100. next eval at 300.

### P2 loss table

| Step | Train | Val | vs P1 best (11.36) | Note |
|------|-------|-----|--------------------|------|
| 100 | 7.82 | 7.72 | -3.64 | descent-shaped |
| 200 | 6.818 | 6.540 | -4.82 | **decider: descent**, no wall, val below train |
| 300 | 5.839 | 5.723 | -5.64 | continued deep descent, -0.82, into the 5s, val below train. no taper |
| 400 | | | | *does it cross 5.33 (the 3B wall value, same vocab)* |

## Update (step 300 landed)

**step 300: train 5.839, val 5.723, down 0.82 from step 200. continued deep descent into the 5s, no taper, val below train.**

**the 8B is now closing on the 3B wall value.** the 3B walled permanently at 5.33. the 8B and 3B share the exact tokenizer (128K base, 44,195 Wake tokens, 26% share), so unlike the Qwen and Mistral comparisons this val is vocab-matched: 5.33 means the same thing to both. the 8B is at 5.723 and dropping roughly 0.8 per hundred steps, so step 400 is where it likely noses below 5.33. if it does, that is the milestone the earlier devlog named: same vocab, same architecture, larger scale, descending past the exact value where the smaller model permanently stopped. 

the suspension test at generation stays its own separate question. but the loss curve is walking the 8B through the 3B's wall on a controlled axis.

### environment note 

Colab pushed a large upgrade (transformers 5.0.0 to 5.12.0, torch 2.10 to 2.11, safetensors 0.7 to 0.8). 
