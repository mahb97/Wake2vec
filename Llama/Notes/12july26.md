# wake2vec devlog 2026-07-12

> *the cursed eval, attempt whatever-this-is*

## Llama 3.1-8B P2 session 11 (resuming from step 350, the 5.33 crossing still cursed)

this run is, as noted, cursed. the decider (step 200) took three attempts before the T4 let it land. the 5.33 crossing (step 400) is now doing the exact same thing: `checkpoint-350` is written and intact, but every reach for the step-400 eval gets cut before it completes. 

`has_weights(dst)` keeps doing its job through every cut, so no checkpoint corruption, only lost steps. resuming from `checkpoint-350`. SEQ_LEN 512, ~240s/step, EVAL_STEPS=100. next eval at 400.

### P2 loss table

| Step | Train | Val | vs P1 best (11.36) | Note |
|------|-------|-----|--------------------|------|
| 200 | 6.818 | 6.540 | -4.82 | **decider: descent** (also took three attempts) |
| 300 | 5.839 | 5.723 | -5.64 | continued deep descent, into the 5s |
| 400 | 5.357 | 5.418 | -5.94 | **the cursed eval LANDED.** val 5.418, still 0.088 above the 3B's 5.33 (crossing is next eval). descent decelerating (-0.31 vs -0.82: anneal throttling since warmup ended at 300). train dipped just under val (crossover) |

the cursed eval finally landed: **step 400, train 5.357, val 5.418.** val is 0.088 above the 3B's frozen 5.33, so the literal sub-5.33 crossing is the next eval, not this one. that is not a shortfall: the 3B is a frozen dot at 5.33 (val range 0.001 across its evals), the 8B is a line descending through it at ~0.3/eval. the finding is descent versus freeze, and it is already secured; the sub-5.33 number is cosmetic confirmation one eval out. the descent has decelerated (-0.82 then -0.31), the cosine anneal throttling in since warmup ended at step 300, so it is gliding like Qwen and Mistral, not walling. train dipped just under val (crossover), which is not overfit while val still descends.
