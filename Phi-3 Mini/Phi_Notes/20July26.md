# wake2vec devlog 2026-07-20

> *both train and val flattening*

## Phi-3.5-mini-instruct P1 session 17 

the train-side slowdown flagged last session is now confirmed. two consecutive shrinking sub-0.1 train drops (0.053 at 1700, 0.027 at 1800) is a trend where the step-1300 lone small drop was noise. so the run has entered its genuine steady state.

resuming from `checkpoint-1800`. best val remains step 300 (12.233), the eventual P2 source.

### P1 loss table

| Step | Train | Val | Note |
|------|-------|-----|------|
| 300 | 11.64 | 12.233 | shallow minimum (best val, P2 source) |
| 1600 | 9.595 | 12.284 | train below 9.6, val pinned |
| 1700 | 9.542 | 12.284 | train drop small (may be slowing) |
| 1800 | 9.515 | 12.285 | train drop 0.027: second sub-0.1, slowdown confirmed |
| 1900 | | | | *next eval* |

### the textbook-resists finding

Phi memorised the training Wake blocks until it ran out of room to memorise harder, and across the whole run held-out validation never left its ~12.28 plateau, a point worse than the uniform-token baseline. no double-dip (step 300 is the global min and P2 source), a modest early-topped val climb (~0.05), and now a saturating train side. the last ~1200 steps trace two nearly flat lines to the end. this is the clearest train-down / val-flat divergence in the lineup after the 8B, and it has reached steady state on schedule.
