# wake2vec devlog 2026-08-19

> *φ*

## Phi-3.5-mini-instruct P2 session 2 

P2 sits on its step-100 surprise and has not been evaluated since: val 11.234, roughly 1.0 below the P1 data wall (12.28) and *below* the ~11.25 uniform-token baseline that P1 never once beat across 3000 steps of memorising. 
LoRA routing extracted something the frozen near-baseline embeddings could not, which beats the naive prediction that the data wall extends into P2 intact. today the run resumes and the second eval says whether that was a descent or a one-eval freebie.

resuming from `checkpoint-100`. LoRA r=8 on the fused targets (qkv_proj, gate_up_proj, down_proj), source P1 step 300 embeddings frozen, LR 2e-5, SEQ_LEN 512, EVAL_STEPS=100. next eval at 200.

### P2 loss table

| Step | Train | Val | Note |
|------|-------|-----|------|
| 100 | 11.252 | 11.234 | descended ~1.0 off the P1 wall, and crossed *below* the ~11.25 baseline P1 never beat. |
| 200 | | | *keeps descending, or walls at ~11.2* |

### for step 200

- **keeps descending** (val into the low 11s or below): routing is doing continuous work off empty embeddings, and the data wall is a P1 phenomenon that LoRA can partially route around at the LM level. the more interesting outcome, because the descent could still be pure FW-surface fitting.
- **walls at ~11.2** (val pinned to the third decimal, the P1 signature repeated one phase up): step 100 was the one-time gain from adapters starting at zero, and the data wall extends into P2 as predicted. the cleanest version of the Phi result.

P2's LoRA trains on FW text, so held-out FW loss can fall on register, punctuation density, and common-token distribution alone, with no Wake-neologism transformation anywhere in it. a data-wall model can route around its empty embeddings and still 
fit the training distribution, so val descending is expected *under the null too*. and the magnitude keeps the whole thing modest: Phi and Mistral are both 32K-vocab, so the numbers are roughly comparable, and Mistral was already in the ~5s this early 
on its way to 4.705. Phi beating its own baseline at 11.23 is not Phi joining the lineup.
