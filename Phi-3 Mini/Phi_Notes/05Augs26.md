# wake2vec devlog 2026-08-05

> *P1 may close today, or not*

## Phi-3.5-mini-instruct P1 session 25 

the last two evals of the run, GPU permitting. last eval, step 2800, train 9.297, val 12.285584 (pinned to the wall). 

resuming from `checkpoint-2800`. best val remains step 300 (12.233).

### P1 loss table

| Step | Train | Val | Note |
|------|-------|-----|------|
| 300 | 11.64 | 12.233 | shallow minimum (best val, P2 source) |
| 2700 | 9.218 | 12.285 | train -0.013 (flattening tease), val dipped within band |
| 2800 | 9.297 | 12.286 | train +0.079 (bounced, flattening refuted), wall holds |
| 2900 | | | | *penultimate eval* |
| 3000 | | | | *P1 close, if the GPU allows* |

across 3000 steps the textbook-trained Phi memorised the training Wake (train from 12.40 into the low 9s, still wobbling down with no clean saturation at 93%) while held-out validation never left its ~12.28 wall, 
a point worse than the uniform-token baseline. no double-dip, best val is the step-300 shallow minimum, the P2 source. 

after step 3000, the post-training analysis suite (norms, isotropy, drift, nearest neighbours, PCA, pairwise cosine) and the P1 generation battery run. the prediction on the geometry: the Wake region should still 
reach ~0.998 isotropy (an attractor, as everywhere else) but with little reorganisation drift, since the val curve says no generalising Wake subspace formed.
