# wake2vec devlog 2026-07-28

> *so excited for those generation samples*

## Phi-3.5-mini-instruct P1 session 21 

into the final ~700 steps: last eval, step 2300, val 12.287 (pinned on the ~12.28 plateau), train 9.309, a 0.074 drop that walked back the premature "saturated" read from step 2200: the train is not saturated, it is on a slow *noisy* descent (-0.078, +0.025, +0.001, -0.074), still going down net but wobbling eval to eval. val flat throughout. 

resuming from `checkpoint-2300`. best val remains step 300 (12.233).

### P1 loss table

| Step | Train | Val | Note |
|------|-------|-----|------|
| 300 | 11.64 | 12.233 | shallow minimum (best val, P2 source) |
| 2100 | 9.382 | 12.284 | 70% through, train +0.025 (noise) |
| 2200 | 9.383 | 12.287 | train flat, looked saturated (held lightly) |
| 2300 | 9.309 | 12.287 | 77% through, train -0.074: not saturated, slow noisy descent |
| 2400 | | | | *next eval* |

the textbook-resists finding is complete: Phi memorised the training Wake blocks (train from 12.40 to the low 9s and still inching down) while held-out validation never left its ~12.28 plateau, a point worse than the uniform-token baseline, 
across the whole run. no double-dip (step 300 is the global min and P2 source), a modest early-topped val climb (~0.05), and a train side descending at a slow noisy clip (never the clean saturation the two-eval reads kept tempting). 
the last ~700 steps trace the flat val line and the noisy train line to 3000. 

Phi versus Mistral: same 58% share, same spherical init, same architecture family, only training data differs. Mistral's internet-trained P1 reorganised the deepest in the lineup and its P2 is at the flat bottom of the 4s 
(best val 4.705, a first overfit-shaped eval at 700 awaiting confirmation). Phi's textbook-trained P1 never left the random-baseline neighbourhood. 
