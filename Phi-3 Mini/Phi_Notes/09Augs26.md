# wake2vec devlog 2026-08-09

> *the completion run, P1 closes today*

## Phi-3.5-mini-instruct P1 session 27

the Phi account GPU has granted ~5 hours, enough to carry the run from `checkpoint-2875` through the last two evals (2900, 3000) and close P1 today. last eval, step 2800, train 9.297 (bounced +0.079, the fourth walk-back of the saturation read), 
val 12.285584 (pinned to the wall). 

resuming from `checkpoint-2875`. eval at 2900, then 3000 closes P1.

### P1 loss table

| Step | Train | Val | Note |
|------|-------|-----|------|
| 300 | 11.64 | 12.233 | shallow minimum (best val, P2 source) |
| 2700 | 9.218 | 12.285 | train -0.013 (flattening tease) |
| 2800 | 9.297 | 12.286 | train +0.079 (flattening refuted), wall holds |
| 2900 | 9.438 | 12.287 | 97% through, train +0.141 (still noisy), val 12.286565 (wall holds) |
| 3000 | 9.211 | 12.288 | **P1 COMPLETE**. train -0.227 to a new low (9.211, lowest of the run, still descending at the final step, never saturated). val 12.288449 (highest, wall holds). gap 3.077, widest at the end. best-val step 300 (12.233), the P2 source |

P1 is done, and the final eval closed it on its strongest note. stated complete: across 3000 steps the textbook-trained Phi memorised the training Wake (train from ~12.40 to 9.211, its new low at the very last step, still descending, never once saturated) while held-out validation never left its ~12.28 wall, a point worse than the uniform-token baseline. the run ended at its maximal divergence, train at a floor it never reached, val at its highest (12.288), gap 3.077. this is the clearest train-down / val-flat divergence in the set after the 8B, and the cleanest "clear wall" in the lineup (pinned to the third decimal, distinct from the 3B's capacity wall: Phi's is a *data* wall, textbook Wake gives nothing to generalise from). that the train was still setting new lows at 3000 is the finding at its sharpest: the model memorised harder to the end and generalised none of it.
