# wake2vec devlog 2026-08-21

> *LoRA r=8 on the fused targets (qkv_proj, gate_up_proj, down_proj), LR 2e-5, SEQ_LEN 512*

## Phi-3.5-mini-instruct P2 session 3 

val 11.234 at step 100, roughly 1.0 below the P1 data wall and below the ~11.25 uniform-token baseline P1 never beat across 3000 steps, on frozen step-300 embeddings that never learned Wake (drift 0.9685, the least in the set). 

resuming from `checkpoint-190`. EVAL_STEPS=100. next eval at 200.

### P2 loss table

| Step | Train | Val | Note |
|------|-------|-----|------|
| 100 | 11.252 | 11.234 | descended ~1.0 off the P1 wall, below the ~11.25 baseline. one eval, and LM descent =/= Wake transformation |
| 200 | 9.792 | 9.811 | val -1.423 (bigger than the step-100 drop, accelerating). 2.42 below the P1 best, 1.44 below the baseline. gap 0.019, train and val descending together |
| 300 | 8.822 | 8.684 | -3.55 | val -1.127, third steep point. gap -0.138 (val below train). held-out now below P1's final train loss (9.211) |

---

| Model | P1 best | P2 step 100 | first-eval drop |
|---|---|---|---|
| Llama 3.1-8B | 11.360 | 7.72 | -3.64 |
| Phi-3.5 | 12.233 | 11.234 | -1.00 |

LoRA initialises B at zero, so the adapters begin as an exact identity and contribute nothing at step 0; part of any first-eval drop is simply the adapters becoming non-trivial at all, 
a one-time transient that every P2 collects and that says nothing about whether the model can route Wake. that transient is spent by 200. whatever 200 shows is closer to the actual slope.

### for 200

- keeps descending: routing is doing continuous work off near-baseline embeddings, and the data wall is a P1 phenomenon LoRA can partly get around at the LM level.
- walls at ~11.2: step 100 was the adapter transient and nothing more, and the wall extends into P2 exactly as predicted. the cleanest version of the Phi result.

P2's LoRA trains on FW text, so held-out FW loss can fall on register, punctuation density, and common-token distribution alone, with no Wake-neologism transformation anywhere in it. a data-wall model can route around empty embeddings and still fit the 
training distribution. val descending is expected under the null too.

Phi against Mistral: same 58% Wake share, same spherical 1.5x init, same family, only pretraining data differs. Mistral's P2 is finishing as the deepest in the set (best val 4.705 at step 600, -6.21 off its P1 minimum, cutting at 1200), out of the 
largest embedding reorganisation in the lineup (drift 0.485, the only Wake region below 0.998 isotropy). Phi is routing from the opposite pole and sitting at 11.23. both 32K-vocab, so the numbers are roughly comparable, and the gap between them is 
currently six and a half points of validation loss on identical data under an identical protocol. 
