# wake2vec devlog 2026-07-10

> *those textbooks must have been good*

## Phi-3.5-mini-instruct P1 session 12 (resuming from step 1300)

no longer just val flat while train falls, it is *both* curves flattening. last eval at step 1300 read train 10.130, val 12.281, and the notable change was the train descent slowing hard (-0.042 that interval, against ~0.18 in the mid-run). the model is running out of training Wake blocks to memorise harder, so even the train side is settling. val stays parked around 12.28, still about a point above the random baseline.

resuming from `checkpoint-1300`. ~1600 steps to P1 end. best val remains step 300 (12.233), the eventual P2 source.

### P1 loss table

| Step | Train | Val | Note |
|------|-------|-----|------|
| 300 | 11.64 | 12.233 | shallow minimum (best val, P2 source) |
| 1000 | 10.510 | 12.277 | 1/3 through, resist ceiling holding |
| 1100 | 10.351 | 12.276 | plateau holds, not a double-dip |
| 1200 | 10.172 | 12.276 | 40% through, val pinned |
| 1300 | 10.130 | 12.281 | train descent slowing hard, both curves flattening |
| 1400 | | | *next eval* |

### the run has essentially delivered

both watch-items are answered and now a third is closing:

1. **double-dip?** none. flat plateau, not a dive. step 300 locked as global min and P2 source.
2. **how high does val climb?** topped out early and modestly (~0.04 above the minimum), then flattened.
3. **does train keep memorising?** slowing sharply now. the memorisation is saturating, so the last ~1600 steps trace two nearly flat lines rather than a widening divergence. the finding does not move, it just stops moving on both axes.

### the 2x2, still decided at generation

Phi versus Mistral: same 58% share, same spherical init, same architecture family, only training data differs. Mistral's internet-trained P1 reorganised the deepest in the lineup and its P2 is in the mid-4s on a clean cosine anneal (best val 4.766 at step 400). Phi's textbook-trained P1 never left the random-baseline neighbourhood. 
