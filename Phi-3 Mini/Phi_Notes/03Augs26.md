# wake2vec devlog 2026-08-03

> *almost at p1 end*

## Phi-3.5-mini-instruct P1 session 24

into the last ~400 steps. last eval, step 2600, train 9.231 (a new low, down -0.242, which gave back the +0.210 up-tick at 2500 and more, so the noisy descent is still running with no saturation this late), 
val 12.287568 (pinned to the third decimal, the clear wall). the train is still digging into the training blocks at 87% through the run. 

resuming from `checkpoint-2600`. 

### P1 loss table

| Step | Train | Val | Note |
|------|-------|-----|------|
| 300 | 11.64 | 12.233 | shallow minimum (best val, P2 source) |
| 2500 | 9.473 | 12.288 | 83% through, train +0.210 (up-wobble), wall holds |
| 2600 | 9.231 | 12.288 | 87% through, train -0.242 (new low, not saturating), wall holds |
| 2700 | 9.218 | 12.285 | 90% through, train -0.013, val -0.002 (dipped within the band, still walled |
| 2800 | 9.297 | 12.286 | 93% through, train +0.079. noisy wobble ~9.2-9.3 to the end. val +0.0004, still walled |

textbook memorises the training Wake (train from 12.40 into the low 9s and still setting new lows at 87%) while held-out validation never leaves the ~12.28 wall, a point worse than the uniform-token baseline. 
best-val is the step-300 shallow minimum, the P2 source. with the original cosine near its floor by 2700, the learning rate is now low enough that the train could finally flatten. 
but 2600 just set a new low, so "flattening" is a weak prediction, and even a late flattening changes nothing about the val wall. the train digging to the end shows how the textbook prior never stops 
memorising and never once generalises.
